<UserControl x:Class="BD_oneLove.controls.NamePhotoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BD_oneLove.controls"
             
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Ellipse Grid.Row="1"  Grid.Column="1">
            <Ellipse.Fill>
                <ImageBrush ImageSource="dailyplanner.jpg"/>
            </Ellipse.Fill>
        </Ellipse>

        <Label Name="Name" Grid.Row="2" Grid.Column="2"  FontWeight="Bold" FontSize="10"   
        Content="{Binding Caption, RelativeSource={RelativeSource FindAncestor, AncestorType=local:NamePhotoControl}, UpdateSourceTrigger=PropertyChanged}"/>

    </Grid>
</UserControl>
